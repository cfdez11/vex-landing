<script client>
  import { useCounter } from "@/utils/context"

  const { counter } = useCounter()
</script>

<template>
  <div class="bg-vex-50 rounded-2xl p-8 border border-vex-100 shadow-sm flex flex-col items-center gap-4">
    <h3 class="text-lg font-semibold text-zinc-800">Visualizador</h3>
    <p class="text-6xl font-extrabold text-vex-600">{{counter.value}}</p>
    <p class="text-sm text-zinc-400">Refleja el estado compartido</p>
  </div>
</template>
