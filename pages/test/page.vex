<script client>
  import Counter from "components/counter.vex"
  import CounterDisplay from "components/counter-display.vex"

  const metadata = {
    title: "Vex — Counter Demo",
    description: "Demostración de estado reactivo compartido entre componentes.",
  };
</script>

<template>
  <div class="min-h-screen bg-zinc-50 flex flex-col items-center justify-center gap-8 p-12">
    <h1 class="text-3xl font-bold text-zinc-900">Estado compartido entre componentes</h1>
    <p class="text-zinc-500 text-center max-w-md">
      Ambos componentes comparten el mismo <code class="font-mono text-xs bg-zinc-100 px-1.5 py-0.5 rounded">counter</code> reactivo desde <code class="font-mono text-xs bg-zinc-100 px-1.5 py-0.5 rounded">utils/context.js</code>.
    </p>
    <div class="grid grid-cols-2 gap-8 w-full max-w-xl">
      <Counter />
      <CounterDisplay />
    </div>
  </div>
</template>
